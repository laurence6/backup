#
# Configure file for backup.sh
#

# Backup these directories
backupdir="/etc /root"

# Exclude these files
exclude=".bash_history,.local/share/Trash,.thumbnails,/etc/fstab,/etc/hostname,*cache*,*Cache*,*tmp*,*.log*,*.old"

# Compressed file extension (gz, xz, bz2)
compressed_ext="gz"

# Backup installed packages list (dpkg, pacman, none)
pkgmgr="dpkg"

# Backup owner
owner="root:root"
